name: hamaraservice_provider
description: HamaraService - Provider App
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.13.1
  firebase_auth: ^4.6.3
  firebase_database: ^10.2.4
  firebase_messaging: ^14.6.7
  shared_preferences: ^2.1.2
  http: ^1.1.0
  geolocator: ^11.0.0
  url_launcher: ^6.2.4
  google_sign_in: ^6.1.6
  geocoding: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
